---
layout: default
title: Home
---

<section class="archive">

<center>
<span class="leg-azul">article</span> &middot
<span class="leg-amber">poster</span> &middot
<span class="leg-avocado">talk</span> &middot
<span class="leg-ruby">misc</span>
</center><br/>

{% for post in site.posts %}
{% unless post.next %}

{% unless forloop.first %}</div></div>{% endunless %}

  <div class="bundle row gutters fadeInDown animated">
    <h2 class="post-year col span_2">{{ post.date | date: '%Y' }}</h2>
    <div class="posts-by-year col span_10">

{% else %}
{% capture year %}{{ post.date | date: '%Y' }}{% endcapture %}
{% capture nyear %}{{ post.next.date | date: '%Y' }}{% endcapture %}
{% if year != nyear %}

{% unless forloop.first %}</div></div>{% endunless %}

  <div class="bundle row gutters fadeInDown animated">
    <h2 class="post-year col span_2">{{ post.date | date: '%Y' }}</h2>
    <div class="posts-by-year col span_10">
{% endif %}
{% endunless %}

  {% assign type=" black" %}
  {% if post.type == "article" %} {% assign type=" azul" %} {% endif %}
  {% if post.type == "poster" %} {% assign type=" amber" %} {% endif %}
  {% if post.type == "talk" %} {% assign type=" avocado" %} {% endif %}
  {% if post.type == "misc" %} {% assign type=" ruby" %} {% endif %}

  <article class="row gutters{{type}}">
    <a href="{{ site.baseurl }}{{ post.url }}" title="{{ post.title }}" class="col span_8">{{ post.title }}</a>
    <div class="post-date col span_4">
      <time datetime="{{ post.date | date: '%Y-%m-%d' }}">{{ post.date | date: "%-d %B" }}</time>
    </div>
  </article>

{% if forloop.last %}</div></div>{% endif %}

{% endfor %}


</section>
